<% @taxons = @taxon && @taxon.parent ? @taxon.parent.children : Spree::Taxon.roots %>

<%= form_tag spree.products_path, :method => :get do %>
    <%= search_field_tag :keywords, params[:keywords], :placeholder => "Find your product here...", class: "form-control" %>
    <span aria-hidden="true" class="glyphicon glyphicon-search form-control-feedback"></span>
<% end %>
